@using EnergyCalculator.Core.Models.Ingredient
@model IEnumerable<AllIngredientViewModel>
@{
    ViewData["Title"] = "All ingredients";
}

<h2 class="text-center">@ViewBag.Title</h2>
<hr />

    @if (Model.Any())
    {
            <div class="row">
            @foreach (var ingr in Model)
            {

                            @*       <div class="col-md-4">
                    <div class="card mb-3">
                        <div class="card-body text-center">
                            <h4>Product:@ingr.</h4>
                              <h4>GrossStandardVolume: @Model.GrossStandardVolume</h4>
                              <h4>Purpose: @Model.Purpose</h4>

                            <h6>DistributionChannel: <b>@Model.DistributionChannel</b></h6>*@

                            <br />
                          }

                       @* </div>
                    </div>
                </div>*@
            }
            </div>
    }
    else
    {
            <h3 class="text-center">You have no ingredients yet!</h3>
    }
